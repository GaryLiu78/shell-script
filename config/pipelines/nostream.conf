# config/pipelines/nostream.conf

# Override these when you want to process another alarm export or write to a fixed file.
NOSTREAM_INPUT_FILE="${NOSTREAM_INPUT_FILE:-$PROJECT_ROOT/modules/alarm_format/data/devicelist}"
NOSTREAM_OUTPUT_FILE="${NOSTREAM_OUTPUT_FILE:-$RESULT_BASE_DIR/nostream/nostream_${RUN_ID}.txt}"

export NOSTREAM_INPUT_FILE NOSTREAM_OUTPUT_FILE
